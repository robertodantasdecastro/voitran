# Todo Next

- iniciar `Etapa 1A`: captura de audio, `VAD` e `STT` local no `macOS`;
- definir benchmark baseline de latencia por etapa;
- integrar `WhisperKit` como trilha principal;
- preparar emissao real de token no backend/control-plane.
